# 智能发票查重 Agent｜作品集完整版交付说明

## 一句话介绍

这是一个面向企业报销审核的可复现原型：优先从发票二维码取得关键字段，再用确定性规则检查批次内重复、历史重复和近似异常；任何字段冲突、权威状态缺失或高风险命中都交给人工复核。

它展示的重点不是“让大模型自动审批”，而是 AI 产品经理如何设计一条有证据、能回归、失败关闭、有人接管的高风险工作流。

## 当前版本

- 交付类型：AI PM 作品集级完整原型
- 版本：v1.0
- 日期：2026-08-23
- 公开范围：仅包含代码、合成/脱敏数据和匿名评测结果
- 不包含：真实票据图片、真实票号、二维码原文、私有黄金标签、账号或密钥

## 已实现能力

| 模块 | 结果 | 正确解读 |
| --- | ---: | --- |
| 二维码结构化提取 | 5/5 三字段全对 | 5 张探索性样本，不代表线上准确率 |
| Tesseract OCR | 票号 4/5、日期 2/5、金额 5/5 | 只适合作为降级候选 |
| 豆包客户端视觉基线 | 票号 2/5、日期 5/5、金额 5/5 | 人工触发，不是 API 集成 |
| 确定性风险规则 | 36/36 | 合成结构化数据回归，不是拦截率 |
| 图片到风险结论演示 | 5 张全部进入人工复核 | 未验真自动放行率为 0% |

5 张图片中有 1 组完全相同的重复图片，共 4 张不同票据。

## 架构结论

本项目采用：

```text
固定 Workflow + 模型局部决策 + 确定性规则 + Human in the Loop
```

不使用多 Agent 动态编排。原因是报销审核步骤固定、输出格式明确，而且财务场景要求可复现、可解释和可审计。

## 推荐阅读顺序

1. `README.md`：项目概览、当前能力和运行方式。
2. `docs/AI_PRD_补充版.md`：完整产品方案、节点契约、异常处理、人工复核、评测和上线门槛。
3. `eval/field_extraction_comparison.md`：二维码、OCR、豆包客户端三条提取路线的对比。
4. `eval/end_to_end_demo_report.md`：5 张图片到风险结论的端到端证据。
5. `eval/README.md`：36 条规则回归的范围和正确口径。
6. `docs/capability-audit.md`：原始 Workflow 存在的问题及本阶段修正。

## 目录说明

```text
智能发票查重Agent_作品集完整版_v1/
├─ 交付说明.md                 本文件
├─ README.md                    项目入口
├─ requirements.txt             最小运行依赖
├─ src/                         可执行代码
│  ├─ audit.py                  确定性风险规则
│  ├─ qr_baseline.py            二维码提取与评测
│  ├─ ocr_baseline.py           Tesseract OCR 基线
│  └─ run_demo.py               图片到风险结论演示
├─ data/                        合成/脱敏演示数据
├─ docs/                        能力审计与完整 AI PRD
├─ eval/                        黄金集、评测脚本和报告
├─ examples/                    结构化规则演示结果
└─ reference/                   原始 Workflow 参考文件
```

## 三分钟复现

在项目根目录运行：

```powershell
python -m pip install -r requirements.txt
python src/audit.py --self-test
python eval/evaluate.py
python src/audit.py --incoming data/incoming_invoices.json --ledger data/historical_ledger.json --output examples/audit_results.json
```

真实图片端到端演示需要本地私有图片和标签，因此公开包不提供原图。运行方法见 `README.md`，输出分为私有明细与公开匿名结果。

## 证据地图

| 面试问题 | 可出示证据 |
| --- | --- |
| 为什么不用大模型直接判断重复？ | `src/audit.py`、`docs/AI_PRD_补充版.md` |
| 为什么二维码优先？ | `eval/field_extraction_comparison.md` |
| 规则是否稳定？ | `eval/golden_set.json`、`eval/results_v1.json` |
| 是否跑通过真实图片？ | `eval/end_to_end_demo_report.md`、匿名结果 JSON |
| 如何处理误识别和冲突？ | AI PRD 的异常分支与字段来源优先级 |
| 为什么不能自动放行？ | R40、端到端报告和项目边界 |
| 豆包是否已经接入？ | `eval/doubao_client_protocol.md`，明确为客户端人工基线 |

## 能力状态

### 已实现并有证据

- 文件哈希批次查重；
- 二维码票号、日期、金额提取；
- 批次票号重复、历史票号重复、近似异常规则；
- 合成黄金集回归；
- 公开结果脱敏；
- 未验真时禁止自动放行。

### 已设计但未实现

- 持久化人工复核队列和审核界面；
- 节点级运行日志、P50/P95 时延和失败率看板；
- OCR/视觉模型自动降级编排；
- 规则与台账版本的完整运行清单。

### 生产前必须补

- 税务权威验真、红冲和作废状态；
- 企业身份、权限、数据加密和审计合规；
- 只读历史台账接口及异常失败关闭；
- 脱敏历史数据试点和 R30 阈值校准；
- 真实人工复核流程与 SLA。

## 面试表达建议

推荐说法：

> 我先验证了三条字段提取路线，发现小样本里二维码比 OCR 和客户端视觉模型更稳定，因此把二维码设为首选，把模型降级为备用。重复判断没有交给大模型，而是拆成可回归的确定性规则。由于没有税务权威验真，我把真实图片流程的自动放行率固定为 0%，所有结论都附规则和证据并交给人工复核。

不要说：

- “已经实现企业自动报销审批”；
- “准确率 100%”；
- “已经接入豆包 API”；
- “已经能识别假票、红冲和作废”；
- “36/36 就代表线上不会漏拦”。

## 发布建议

本压缩包可直接作为面试材料发送。若公开到 GitHub，建议只上传本包内容，并在仓库描述中使用“作品集原型”“合成/脱敏数据”“未接入税务验真”等限定词。

GitHub 同步不是本次交付的必要条件；本地包验证通过后再发布，可以避免把临时文件或未经确认的表述带入公开仓库。
